import React from 'react';

function Title({count}) {
    return ( 
        <div>
            <h1>Semana OmniStock</h1>
            <h1>{count}</h1>
        </div>
    );
    }

    export default Title;